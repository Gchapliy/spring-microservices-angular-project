<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.0.xsd http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd"
        xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://www.liquibase.org/xml/ns/dbchangelog">

    <changeSet id="1" author="hrygorii">
        <sql>
            CREATE TABLE course (
            id SERIAL NOT NULL PRIMARY KEY,
            title VARCHAR(255) NOT NULL,
            author VARCHAR(255) NOT NULL,
            category VARCHAR(255),
            publish_date DATE);
        </sql>
        <rollback>DROP TABLE course;</rollback>
    </changeSet>
    <changeSet author="hrygorii" id="2">
        <sql>
            CREATE TABLE transaction (
            id SERIAL NOT NULL PRIMARY KEY,
            course_id BIGINT NOT NULL,
            user_id BIGINT NOT NULL,
            date_of_issue TIMESTAMP,
            CONSTRAINT fk_tran_course FOREIGN KEY (course_id) REFERENCES course(id) ON DELETE CASCADE ON UPDATE
            CASCADE);
        </sql>
        <rollback>DROP TABLE transaction;</rollback>
    </changeSet>
    <changeSet author="hrygorii" id="3">
        <sql>
            INSERT INTO course (title, author, category, publish_date) VALUES('Microservices', 'Instructor 1',
            'Programming', NOW());
            INSERT INTO course (title, author, category, publish_date) VALUES('Java Programming',
            'Instructor 2', 'Programming', NOW());
            INSERT INTO course (title, author, category, publish_date) VALUES('Web
            Development', 'Instructor 3', 'Web', NOW());
            INSERT INTO course (title, author, category, publish_date)
            VALUES('Mobile Application', 'Instructor 4', 'Mobile', NOW());
            INSERT INTO course (title, author, category,
            publish_date) VALUES('Amazon Web Services', 'Instructor 5', 'Administration', NOW());
        </sql>
        <rollback>TRUNCATE TABLE course;</rollback>
    </changeSet>
</databaseChangeLog>
